<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dùng khi API < 31 (Android 12) -->
    <style name="LaunchTheme" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <!-- Nền hiển thị trước khi Flutter vẽ frame đầu -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@style/Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
